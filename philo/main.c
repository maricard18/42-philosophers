

#include "philosophers.h"

int main(int argc, char **argv)
{
    t_root  root;

    root = NULL;
    if (argc < 5)
    {
        pritnf("Program must receive at least 4 arguments to start\n");
        return (0);
    }
}
